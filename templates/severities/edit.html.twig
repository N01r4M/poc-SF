{% extends 'base.html.twig' %}

{% block title %}{{ severity.name }} | {% endblock %}

{% block body %}
    <h1>Modification | {{ severity.name }}</h1>

    <div class="d-flex justify-content-between">
        <a href="{{ path('app_severities_index') }}">Retour à la liste</a>
        {{ include('severities/_delete_form.html.twig') }}
    </div>

    {{ include('severities/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
{% endblock %}
