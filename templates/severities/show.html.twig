{% extends 'base.html.twig' %}

{% block title %}Severities{% endblock %}

{% block body %}
    <h1>Severities</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ severity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ severity.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_severities_index') }}">back to list</a>

    <a href="{{ path('app_severities_edit', {'id': severity.id}) }}">edit</a>

    {{ include('severities/_delete_form.html.twig') }}
{% endblock %}
