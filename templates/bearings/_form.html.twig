{{ form_start(form) }}
    <div>
        {{ form_errors(form) }}
    </div>

    {{ form_row(form.name) }}
    {{ form_row(form.isMandatory) }}

    <button class="btn">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
