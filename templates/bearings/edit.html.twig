{% extends 'base.html.twig' %}

{% block title %}{{ bearing.name }} | {% endblock %}

{% block body %}
    <h1>Modification | {{ bearing.name }}</h1>

    <div class="d-flex justify-content-between">
        <a href="{{ path('app_bearings_index') }}">Retour à la liste</a>
        {{ include('bearings/_delete_form.html.twig') }}
    </div>

    {{ include('bearings/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
{% endblock %}
