<form method="post" action="{{ path('app_bearings_delete', {'id': bearing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bearing.id) }}">
    <button class="btn">Delete</button>
</form>
