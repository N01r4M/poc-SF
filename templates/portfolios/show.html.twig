{% extends 'base.html.twig' %}

{% block title %}Portfolios{% endblock %}

{% block body %}
    <h1>Portfolios</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ portfolio.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ portfolio.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_portfolios_index') }}">back to list</a>

    <a href="{{ path('app_portfolios_edit', {'id': portfolio.id}) }}">edit</a>

    {{ include('portfolios/_delete_form.html.twig') }}
{% endblock %}
