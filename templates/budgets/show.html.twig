{% extends 'base.html.twig' %}

{% block title %}Budgets{% endblock %}

{% block body %}
    <h1>Budgets</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ budget.id }}</td>
            </tr>
            <tr>
                <th>InitialValue</th>
                <td>{{ budget.initialValue }}</td>
            </tr>
            <tr>
                <th>ConsumedValue</th>
                <td>{{ budget.consumedValue }}</td>
            </tr>
            <tr>
                <th>StillToDo</th>
                <td>{{ budget.stillToDo }}</td>
            </tr>
            <tr>
                <th>Landing</th>
                <td>{{ budget.landing }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_budgets_index') }}">back to list</a>

    <a href="{{ path('app_budgets_edit', {'id': budget.id}) }}">edit</a>

    {{ include('budgets/_delete_form.html.twig') }}
{% endblock %}
